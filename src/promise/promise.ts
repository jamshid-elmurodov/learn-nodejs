// promise return type functions then/catch

// async/await function
// ()()
// Promise methods all, race, allSettled, any

// const all = Promise.race([
//     Promise.reject(new Error(`Geocode.xyz`)),
//     Promise.resolve("hello"),
//     Promise.resolve("jjjj")
// ]);
//
// (async () => {
//     await all;
// })()
//